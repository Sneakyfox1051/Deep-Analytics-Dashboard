# Deep-Analytics-Dashboard

- **Backend (API):** deploy the `backend` folder on **[Render](https://render.com)** — see [`backend/README.md`](backend/README.md).
- **Frontend:** static files in `frontend/` (e.g. Netlify with `frontend/netlify.toml` proxying `/api` to your Render URL).
