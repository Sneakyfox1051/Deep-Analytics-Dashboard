{
  "name": "awwa-sheets-api",
  "version": "1.0.0",
  "description": "Backend API for AWWA Dashboard - fetches data from Google Sheets",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node -e \"require('dotenv').config(); require('nodemon')({ exec: 'node server.js' })\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "googleapis": "^128.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
